export const PATHS = {
  MAIN: "/",
  STATUS: "/status",
  STOCK: "/stock",
} as const;

export const ROUTES = {
  MAIN: PATHS.MAIN,
  STATUS: PATHS.STATUS,
  STOCK: PATHS.STOCK,
} as const;

export const AMOUNT_NEWS_VISIBLE = 6;
